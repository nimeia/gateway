package gateway.worker.predicate;

import java.util.function.Predicate;

public class TestPredicate implements Predicate {
    @Override
    public boolean test(Object o) {
        return true;
    }
}
